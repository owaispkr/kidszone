import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  spacing: 4,
  palette: {
    primary: { main: "#467fcf" },
    background: {
      default: "#EDE7E3",
    },
  },
});

export default theme;
